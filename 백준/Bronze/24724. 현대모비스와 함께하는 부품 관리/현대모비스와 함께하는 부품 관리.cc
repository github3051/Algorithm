#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;


int main()
{
	int t;
	cin >> t;
	for (int i = 1; i <= t; ++i)
	{
		int n,a,b;
		cin >> n >> a >> b;
		for (int j = 0; j < n; ++j)
		{
			int tmpA, tmpB;
			cin >> tmpA >> tmpB;
		}
		cout << "Material Management " << i << "\nClassification ---- End!\n";
	}
}